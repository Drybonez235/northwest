{% extends "base.twig" %}

{% block content %}
  <article class="post-type-{{ post.post_type }}" id="post-{{ post.ID }}">
    
    {# This renders the content from your HTML block exactly as written #}
    <div class="raw-html-content">
      {{ post.content }}
    </div>

  </article>
{% endblock %}